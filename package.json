{
  "name": "halvfems",
  "homepage": "http://unorsk.cc",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "parcel": "^1.12.3",
    "react": "^16.4.2",
    "react-dom": "^16.4.2"
  },
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "predeploy": "npm run build --public-url https://photos.unorsk.cc",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
